# Port for the webhook relay server (default: 8443)
PORT=8443

# Shared secret - Cloudflare sends this in X-Webhook-Secret header
# Generate: openssl rand -hex 32
WEBHOOK_SECRET=

# GitHub webhook secret (configured in GitHub repo webhook settings)
# Generate: openssl rand -hex 32
GITHUB_WEBHOOK_SECRET=

# Krisp webhook secret (sent as Authorization: Bearer header)
KRISP_WEBHOOK_SECRET=

# Directory for storing Krisp meeting data (default: data/krisp)
# KRISP_DATA_DIR=data/krisp

# TLS - file paths to PEM files
# If both are set, the server starts with HTTPS. Otherwise plain HTTP.
# Example:
# TLS_CERT=/Users/<user>/certs/origin-cert.pem
# TLS_KEY=/Users/<user>/certs/origin-key.pem
TLS_CERT=
TLS_KEY=

# Optional: CA certificate path (e.g. Cloudflare Origin CA root)
TLS_CA=

# OpenClaw integration - wake the main session on webhook events
# URL of the OpenClaw gateway (default: http://127.0.0.1:18789)
OPENCLAW_URL=http://127.0.0.1:18789
# Gateway API token (from openclaw.json or `openclaw status`)
OPENCLAW_TOKEN=
